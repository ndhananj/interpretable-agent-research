name: replace_token
description: Replace TODO with DONE and verify the file contents.
fixture_dir: tasks/fixtures/replace_token
work_dir_name: work
instruction: Replace TODO with DONE in input.txt, then verify the result.
allowed_commands:
  - python3
checks:
  - type: file_contains
    path: input.txt
    text: DONE
  - type: file_not_contains
    path: input.txt
    text: TODO
max_score: 1.0

