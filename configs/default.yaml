experiment:
  trials: 3
  run_root: runs
  baseline_functionality: null
  functionality_floor_ratio: 0.90
  candidate_seed: 7

model:
  backend: mock
  name: Qwen/Qwen2.5-Coder-1.5B-Instruct
  local_gguf: /media/nithin/data2/nithin/codex_work/linux_skills/runtime/models/qwen2.5-1.5b-instruct-q4_k_m.gguf
  use_vllm_if_available: true

resources:
  conservative: true
  max_cpu_threads: 4
  max_load_ratio: 0.75
  min_available_memory_gib: 8
  task_timeout_s: 45
  experiment_timeout_s: 240

scoring:
  metrics_config: configs/metrics.yaml
  task_paths:
    - tasks/replace_token.yaml

